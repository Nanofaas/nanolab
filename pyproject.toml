[project]
name = "nanolab-workspace"
license = "MIT"
license-files = ["LICENSE"]
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "sonata-engine==0.6.2",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "packages/nanolab",
    "packages/tui-toolkit",
]
