"""The Prometheus query catalogue a benchmark run is measured with.

Names the series to collect, grouped by the component that publishes them, so
an empty result is a fact about the run rather than about the question.
"""
